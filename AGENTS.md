# AGENTS.md — MAYA (AI Executive Partner)

実装方針は `CODEX_BOOTSTRAP.md` と `docs/` 配下の各仕様書を参照。まず `README.md` → `docs/PRODUCT_REQUIREMENTS.md` → ... の順に読むこと（README記載の順序）。

---

## Content Engine: CONTENT_LOG運用ルール

開発中に新機能・大きな仕様変更・問題解決・技術選定・失敗・大きな改善・再利用可能な知見など、
外部コンテンツとして価値のある出来事が発生した場合、このプロジェクト直下の `CONTENT_LOG.md` に追記する。
trivialな変更（typo・依存パッケージ更新のみ等）は記録不要。CONTENT_LOG更新のために開発作業を大きく遅延させない。

記録ルールの詳細は `C:\Users\koji3\Documents\content-engine\docs\content-log-guidelines.md` を参照
（アカウント名・APIキー・具体的な商品名・ブランド名は書かない等の制約あり）。

---

## 構成の手引き: ARCHITECTURE_GUIDE.html の更新ルール

リポジトリ直下の `ARCHITECTURE_GUIDE.html` は、技術者でない読み手（社長本人）向けに
構成を図解した1枚の手引き。正本は `docs/PLATFORM_ARCHITECTURE.md` で、こちらはその平易版。

**図が変わるような改修をしたら、同じコミットでこのファイルも直す。**
判断に迷わないよう、更新が要る変更を具体的に挙げる。

- 構成要素が増える・消える（サーバー、データベース、外部サービス、同期ジョブ）
- データの流れが変わる（誰が誰を呼ぶか、どこを経由するか）
- 鍵・認証の置き場所や入口の守り方が変わる
- 手引きの「これから作る部分」が動くようになった、または取りやめた
- 「できたこと・これから」の表の状態が変わった
- 既定のモデルや無料枠の前提が変わった
- 利用者から見た操作の意味が変わった（設定項目が効かなくなる等）

次は更新不要。画面の文言、内部リファクタ、テスト追加、依存更新、素材の差し替え。

**公開のしかた**

このファイルはアーティファクトとして公開済み。**必ず同じアドレスへ公開し直す。**
新しく publish すると別のアーティファクトができて、社長の手元のリンクが古いままになる。

```
URL: https://claude.ai/code/artifact/aebe739f-d47d-45d7-95a2-2bb64b276bdb
```

Artifact ツールに `url` としてこのアドレスを渡す（この会話の外から更新する場合は必須）。
公開前に `action: "read"` でいまの版を読み、その上に変更を重ねること。

**書き方の約束**

- 専門用語は、使うなら必ずその場で言い換える。読み手は技術者ではない
- 図は手書きの SVG。`currentColor` を使い、強調色は `<g class="hl">` に載せる
  （明暗どちらのテーマでも読めるようにするため）
- 裏の取れていない数字は「未確認」と明記する。埋めない
- 実測値があるものは実測値を書く。推定と実測を混ぜない
