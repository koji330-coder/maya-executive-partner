# Do not place production secrets in the mobile app.
# Only EXPO_PUBLIC_* variables are readable from client code, and they are
# embedded in the bundle. Never put provider API keys here.
EXPO_PUBLIC_API_BASE_URL=
EXPO_PUBLIC_ENV=development
